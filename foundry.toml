[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
via_ir = true

[fuzz]
runs = 1000

[invariant]
runs = 256
depth = 15

[etherscan.base-sepolia]
key = "${BASESCAN_API_KEY}"
url = "https://api-sepolia.basescan.org/api"
